root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/app.exe ./cmd/server"
  bin = "tmp/app.exe"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "testdata"]
  exclude_regex = ["_test.go"]
  delay = 1000
  log = "build-errors.log"

[log]
  time = true
